<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        id="idCreateDialog"
        type="Message"
        title="New City"
        beforeClose="onBeforeClose"
    >
        <content>
            <VBox class="sapUiSmallMarginBottom">
                <Label
                    text="City"
                    labelFor="idNameInput"
                />
                <Input
                    id="idNameInput"
                    class="sapUiSmallMarginEnd"
                />
            </VBox>
            <VBox class="sapUiSmallMarginBottom">
                <Label
                    text="Area"
                    labelFor="idAreaInput"
                />
                <Input id="idAreaInput" />
            </VBox>
            <VBox>
                <Label
                    text="Population"
                    labelFor="idPopInput"
                />
                <Input id="idPopInput" />
            </VBox>
        </content>
        <beginButton>
            <Button
                text="Create"
                type="Emphasized"
                press="onCreate"
            />
        </beginButton>
        <endButton>
            <Button
                text="Cancel"
                press="onCancel"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
